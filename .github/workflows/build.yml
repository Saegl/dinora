name: Package Application with PyInstaller

on:
  push:
    tags:
      - 'v*'  # Triggers only on version tags like v1.0.0
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install -r py_installer/requirements_onnx_cpu.txt

      - name: Build executable
        run: |
          pyinstaller py_installer/dinora_onnx_cpu.spec

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dinora-onnx-cpu-${{ matrix.os }}
          path: dist/dinora*
